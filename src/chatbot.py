from chat_bot import ChatBot
#Punto de entrada principal del programa.
if __name__ == "__main__":
    #Crea una instancia del ChatBot y comienza la interacción con el usuario llamando al método `iniciar_chat()`.
    chatbot = ChatBot()
    chatbot.iniciar_chat()
